/// SDK version constant. Update this alongside pubspec.yaml when releasing.
/// The prepare-release workflow (.github/workflows/prepare-release.yml) edits
/// this file via the version_files entry in .github/modules.json.
const String sdkVersion = '2.11.0';
