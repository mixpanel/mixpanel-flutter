name: mixpanel_flutter_common
description: Shared common library for the Mixpanel Flutter SDK family.
version: 1.0.0
homepage: https://mixpanel.com
repository: https://github.com/mixpanel/mixpanel-flutter/tree/main/packages/mixpanel_flutter_common
issue_tracker: https://github.com/mixpanel/mixpanel-flutter/issues
documentation: https://developer.mixpanel.com/docs/flutter

topics:
  - mixpanel
  - analytics

environment:
  # Matches mixpanel_flutter's published SDK floor.
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  meta: ^1.8.0

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
